<!DOCTYPE html>
<html lang="bn">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ক্লায়েন্ট ম্যানেজমেন্ট সিস্টেম</title>
    <style>
        body {
            box-sizing: border-box;
            font-family: 'Inter', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 20px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            color: #333;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.15);
            overflow: hidden;
        }

        .header {
            background: linear-gradient(135deg, #2c3e50 0%, #34495e 100%);
            color: white;
            padding: 30px;
            text-align: center;
        }

        .header h1 {
            margin: 0;
            font-size: 28px;
            font-weight: 700;
        }

        .header p {
            margin: 10px 0 0 0;
            opacity: 0.9;
            font-size: 16px;
        }

        .main-content {
            padding: 30px;
        }

        .add-client-section {
            background: linear-gradient(135deg, #f8fafc 0%, #e8ecf4 100%);
            padding: 25px;
            border-radius: 16px;
            margin-bottom: 30px;
            border: 2px solid #e8ecf4;
        }

        .section-title {
            font-size: 20px;
            font-weight: 700;
            color: #2c3e50;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
        }

        .section-title::before {
            content: "➕";
            margin-right: 10px;
            font-size: 18px;
        }

        .form-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
            margin-bottom: 20px;
        }

        .form-group {
            display: flex;
            flex-direction: column;
        }

        .form-group label {
            font-weight: 600;
            color: #2c3e50;
            margin-bottom: 8px;
            font-size: 14px;
        }

        .form-group input,
        .form-group textarea {
            padding: 12px 16px;
            border: 2px solid #e8ecf4;
            border-radius: 12px;
            font-size: 14px;
            transition: all 0.3s ease;
            background: white;
        }

        .form-group input:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 0 0 4px rgba(102, 126, 234, 0.1);
            transform: translateY(-1px);
        }

        .form-group textarea {
            resize: vertical;
            min-height: 80px;
        }

        .add-btn {
            background: linear-gradient(135deg, #10b981 0%, #059669 100%);
            color: white;
            border: none;
            padding: 14px 28px;
            border-radius: 12px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(16, 185, 129, 0.3);
        }

        .add-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(16, 185, 129, 0.4);
        }

        .clients-section {
            margin-top: 40px;
            background: linear-gradient(135deg, #f8fafc 0%, #e8ecf4 100%);
            padding: 30px;
            border-radius: 20px;
            border: 2px solid #e8ecf4;
            box-shadow: 0 10px 30px rgba(0,0,0,0.08);
        }

        .clients-title {
            font-size: 24px;
            font-weight: 800;
            color: #2c3e50;
            margin-bottom: 25px;
            display: flex;
            align-items: center;
            text-align: center;
            justify-content: center;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            position: relative;
        }

        .clients-title::before {
            content: "👥";
            margin-right: 15px;
            font-size: 28px;
            -webkit-text-fill-color: initial;
            background: none;
        }

        .clients-title::after {
            content: "";
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
            width: 80px;
            height: 4px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 2px;
        }

        .search-container {
            display: flex;
            justify-content: center;
            margin-bottom: 30px;
        }

        .search-box {
            width: 100%;
            max-width: 500px;
            padding: 16px 20px;
            border: 3px solid #e8ecf4;
            border-radius: 25px;
            font-size: 16px;
            transition: all 0.3s ease;
            background: white;
            box-shadow: 0 5px 15px rgba(0,0,0,0.08);
            font-weight: 500;
        }

        .search-box:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 0 0 4px rgba(102, 126, 234, 0.15), 0 8px 25px rgba(0,0,0,0.12);
            transform: translateY(-2px);
        }

        .search-box::placeholder {
            color: #94a3b8;
            font-weight: 400;
        }

        .clients-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
            gap: 20px;
        }

        .client-card {
            background: white;
            border: 2px solid #e8ecf4;
            border-radius: 16px;
            padding: 20px;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(0,0,0,0.05);
        }

        .client-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.1);
            border-color: #667eea;
        }

        .client-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
            padding-bottom: 15px;
            border-bottom: 2px solid #f1f5f9;
        }

        .client-name {
            font-size: 18px;
            font-weight: 700;
            color: #2c3e50;
        }

        .client-actions {
            display: flex;
            gap: 8px;
        }

        .action-btn {
            padding: 6px 12px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-size: 12px;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .edit-btn {
            background: #3b82f6;
            color: white;
        }

        .edit-btn:hover {
            background: #2563eb;
            transform: scale(1.05);
        }

        .delete-btn {
            background: #ef4444;
            color: white;
        }

        .delete-btn:hover {
            background: #dc2626;
            transform: scale(1.05);
        }

        .download-btn {
            background: #10b981;
            color: white;
        }

        .download-btn:hover {
            background: #059669;
            transform: scale(1.05);
        }

        .client-info {
            display: grid;
            gap: 12px;
        }

        .info-group {
            background: #f8fafc;
            padding: 12px;
            border-radius: 8px;
            border-left: 4px solid #667eea;
        }

        .info-label {
            font-size: 12px;
            font-weight: 600;
            color: #64748b;
            text-transform: uppercase;
            margin-bottom: 4px;
        }

        .info-value {
            font-size: 14px;
            color: #2c3e50;
            word-break: break-all;
        }

        .password-field {
            position: relative;
        }

        .toggle-password {
            position: absolute;
            right: 12px;
            top: 50%;
            transform: translateY(-50%);
            background: none;
            border: none;
            cursor: pointer;
            font-size: 16px;
            color: #64748b;
        }

        .empty-state {
            text-align: center;
            padding: 60px 20px;
            color: #64748b;
        }

        .empty-state h3 {
            font-size: 20px;
            margin-bottom: 10px;
        }

        .required {
            color: #ef4444;
        }

        @media (max-width: 768px) {
            .form-grid {
                grid-template-columns: 1fr;
            }
            
            .clients-grid {
                grid-template-columns: 1fr;
            }
            
            .main-content {
                padding: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🌐 ক্লায়েন্ট ম্যানেজমেন্ট সিস্টেম</h1>
            <p>ওয়েবসাইট এবং হোস্টিং তথ্য সংরক্ষণ ও ব্যবস্থাপনা</p>
        </div>

        <div class="main-content">
            <!-- Add Client Section -->
            <div class="add-client-section">
                <div class="section-title">নতুন ক্লায়েন্ট যোগ করুন</div>
                
                <form id="clientForm">
                    <div class="form-grid">
                        <div class="form-group">
                            <label for="clientName">ক্লায়েন্টের নাম <span class="required">*</span></label>
                            <input type="text" id="clientName" required placeholder="ক্লায়েন্টের পূর্ণ নাম">
                        </div>
                        
                        <div class="form-group">
                            <label for="websiteUrl">ক্লায়েন্টের ওয়েবসাইট <span class="required">*</span></label>
                            <input type="url" id="websiteUrl" required placeholder="https://example.com">
                        </div>
                        
                        <div class="form-group">
                            <label for="websiteUser">ওয়েবসাইট ইউজারনেম <span class="required">*</span></label>
                            <input type="text" id="websiteUser" required placeholder="ওয়েবসাইট অ্যাডমিন ইউজারনেম">
                        </div>
                        
                        <div class="form-group">
                            <label for="websitePass">ওয়েবসাইট পাসওয়ার্ড <span class="required">*</span></label>
                            <div class="password-field">
                                <input type="password" id="websitePass" required placeholder="ওয়েবসাইট অ্যাডমিন পাসওয়ার্ড">
                                <button type="button" class="toggle-password" onclick="togglePassword('websitePass')">👁️</button>
                            </div>
                        </div>
                        
                        <div class="form-group">
                            <label for="cpanelUser">cPanel ইউজারনেম <span class="required">*</span></label>
                            <input type="text" id="cpanelUser" required placeholder="cPanel ইউজারনেম">
                        </div>
                        
                        <div class="form-group">
                            <label for="cpanelPass">cPanel পাসওয়ার্ড <span class="required">*</span></label>
                            <div class="password-field">
                                <input type="password" id="cpanelPass" required placeholder="cPanel পাসওয়ার্ড">
                                <button type="button" class="toggle-password" onclick="togglePassword('cpanelPass')">👁️</button>
                            </div>
                        </div>
                        
                        <div class="form-group">
                            <label for="nameserver1">নেমসার্ভার ১ <span class="required">*</span></label>
                            <input type="text" id="nameserver1" required placeholder="ns1.example.com">
                        </div>
                        
                        <div class="form-group">
                            <label for="nameserver2">নেমসার্ভার ২ <span class="required">*</span></label>
                            <input type="text" id="nameserver2" required placeholder="ns2.example.com">
                        </div>
                        
                        <div class="form-group">
                            <label for="serviceProvider">সার্ভিস প্রোভাইডারের নাম <span class="required">*</span></label>
                            <input type="text" id="serviceProvider" required placeholder="যেমন: Hostinger, GoDaddy, Namecheap">
                        </div>
                    </div>
                    
                    <div class="form-group">
                        <label for="notes">অতিরিক্ত নোট</label>
                        <textarea id="notes" placeholder="কোন বিশেষ তথ্য বা নির্দেশনা..."></textarea>
                    </div>
                    
                    <button type="submit" class="add-btn">
                        ✅ ক্লায়েন্ট যোগ করুন
                    </button>
                </form>
            </div>

            <!-- Clients List Section -->
            <div class="clients-section">
                <div class="clients-title">সংরক্ষিত ক্লায়েন্ট তালিকা</div>
                
                <div class="search-container">
                    <input type="text" class="search-box" id="searchBox" placeholder="🔍 ক্লায়েন্টের নাম বা ওয়েবসাইট দিয়ে খুঁজুন...">
                </div>
                
                <div class="clients-grid" id="clientsGrid">
                    <div class="empty-state">
                        <h3>📋 কোন ক্লায়েন্ট নেই</h3>
                        <p>উপরের ফর্ম ব্যবহার করে প্রথম ক্লায়েন্ট যোগ করুন</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        let clients = JSON.parse(localStorage.getItem('clients')) || [];
        let editingId = null;

        function saveClients() {
            localStorage.setItem('clients', JSON.stringify(clients));
        }

        function generateId() {
            return Date.now().toString(36) + Math.random().toString(36).substr(2);
        }

        function togglePassword(fieldId) {
            const field = document.getElementById(fieldId);
            const button = field.nextElementSibling;
            
            if (field.type === 'password') {
                field.type = 'text';
                button.textContent = '🙈';
            } else {
                field.type = 'password';
                button.textContent = '👁️';
            }
        }

        function addClient(clientData) {
            const client = {
                id: generateId(),
                ...clientData,
                createdAt: new Date().toISOString()
            };
            
            clients.push(client);
            saveClients();
            renderClients();
        }

        function updateClient(id, clientData) {
            const index = clients.findIndex(c => c.id === id);
            if (index !== -1) {
                clients[index] = { ...clients[index], ...clientData };
                saveClients();
                renderClients();
            }
        }

        function deleteClient(id) {
            if (confirm('আপনি কি নিশ্চিত যে এই ক্লায়েন্টের তথ্য মুছে ফেলতে চান?')) {
                clients = clients.filter(c => c.id !== id);
                saveClients();
                renderClients();
            }
        }

        function editClient(id) {
            const client = clients.find(c => c.id === id);
            if (!client) return;

            editingId = id;
            
            // Fill form with client data
            document.getElementById('clientName').value = client.name || '';
            document.getElementById('websiteUrl').value = client.website || '';
            document.getElementById('websiteUser').value = client.websiteUser || '';
            document.getElementById('websitePass').value = client.websitePass || '';
            document.getElementById('cpanelUser').value = client.cpanelUser || '';
            document.getElementById('cpanelPass').value = client.cpanelPass || '';
            document.getElementById('nameserver1').value = client.nameserver1 || '';
            document.getElementById('nameserver2').value = client.nameserver2 || '';
            document.getElementById('serviceProvider').value = client.serviceProvider || '';
            document.getElementById('notes').value = client.notes || '';
            
            // Change button text
            document.querySelector('.add-btn').textContent = '✏️ ক্লায়েন্ট আপডেট করুন';
            
            // Scroll to form
            document.querySelector('.add-client-section').scrollIntoView({ behavior: 'smooth' });
        }

        function renderClients() {
            const grid = document.getElementById('clientsGrid');
            const searchTerm = document.getElementById('searchBox').value.toLowerCase();
            
            const filteredClients = clients.filter(client => 
                client.name.toLowerCase().includes(searchTerm) ||
                client.website.toLowerCase().includes(searchTerm)
            );
            
            if (filteredClients.length === 0) {
                grid.innerHTML = `
                    <div class="empty-state">
                        <h3>📋 ${searchTerm ? 'কোন ক্লায়েন্ট পাওয়া যায়নি' : 'কোন ক্লায়েন্ট নেই'}</h3>
                        <p>${searchTerm ? 'অন্য কিছু খুঁজে দেখুন' : 'উপরের ফর্ম ব্যবহার করে প্রথম ক্লায়েন্ট যোগ করুন'}</p>
                    </div>
                `;
                return;
            }
            
            grid.innerHTML = filteredClients.map(client => `
                <div class="client-card">
                    <div class="client-header">
                        <div class="client-name">${client.name}</div>
                        <div class="client-actions">
                            <button class="action-btn edit-btn" onclick="editClient('${client.id}')">✏️ সম্পাদনা</button>
                            <button class="action-btn delete-btn" onclick="deleteClient('${client.id}')">🗑️ মুছুন</button>
                            <button class="action-btn download-btn" onclick="downloadClientPDF('${client.id}')">📄 PDF</button>
                        </div>
                    </div>
                    
                    <div class="client-info">
                        <div class="info-group">
                            <div class="info-label">ওয়েবসাইট</div>
                            <div class="info-value">
                                <a href="${client.website}" target="_blank" style="color: #3b82f6; text-decoration: none;">
                                    ${client.website}
                                </a>
                            </div>
                        </div>
                        
                        <div class="info-group">
                            <div class="info-label">ওয়েবসাইট লগইন</div>
                            <div class="info-value">
                                <strong>ইউজার:</strong> ${client.websiteUser}<br>
                                <strong>পাস:</strong> <span style="font-family: monospace;">••••••••</span>
                                <button onclick="showPassword('${client.websitePass}')" style="margin-left: 8px; background: #e5e7eb; border: none; padding: 2px 6px; border-radius: 4px; cursor: pointer; font-size: 12px;">দেখুন</button>
                            </div>
                        </div>
                        
                        <div class="info-group">
                            <div class="info-label">cPanel লগইন</div>
                            <div class="info-value">
                                <strong>ইউজার:</strong> ${client.cpanelUser}<br>
                                <strong>পাস:</strong> <span style="font-family: monospace;">••••••••</span>
                                <button onclick="showPassword('${client.cpanelPass}')" style="margin-left: 8px; background: #e5e7eb; border: none; padding: 2px 6px; border-radius: 4px; cursor: pointer; font-size: 12px;">দেখুন</button>
                            </div>
                        </div>
                        
                        <div class="info-group">
                            <div class="info-label">নেমসার্ভার</div>
                            <div class="info-value">
                                NS1: ${client.nameserver1}<br>
                                NS2: ${client.nameserver2}
                            </div>
                        </div>
                        
                        <div class="info-group">
                            <div class="info-label">সার্ভিস প্রোভাইডার</div>
                            <div class="info-value">${client.serviceProvider}</div>
                        </div>
                        
                        ${client.notes ? `
                        <div class="info-group">
                            <div class="info-label">নোট</div>
                            <div class="info-value">${client.notes}</div>
                        </div>
                        ` : ''}
                    </div>
                </div>
            `).join('');
        }

        function showPassword(password) {
            alert(`পাসওয়ার্ড: ${password}`);
        }

        function downloadClientPDF(clientId) {
            const client = clients.find(c => c.id === clientId);
            if (!client) return;

            // Create PDF content
            const pdfContent = `<!DOCTYPE html>
<html lang="bn">
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; margin: 40px; color: #333; }
        .header { text-align: center; margin-bottom: 40px; border-bottom: 3px solid #667eea; padding-bottom: 20px; }
        .company-name { font-size: 28px; font-weight: bold; color: #667eea; margin-bottom: 5px; }
        .company-contact { font-size: 16px; color: #666; }
        .client-title { font-size: 24px; font-weight: bold; margin: 30px 0 20px 0; color: #2c3e50; }
        .info-section { margin: 20px 0; padding: 20px; background: #f8fafc; border-radius: 10px; border-left: 5px solid #667eea; }
        .info-title { font-size: 18px; font-weight: bold; color: #2c3e50; margin-bottom: 15px; }
        .info-item { margin: 10px 0; font-size: 16px; }
        .info-label { font-weight: bold; color: #555; display: inline-block; width: 150px; }
        .info-value { color: #333; }
        .footer { margin-top: 50px; text-align: center; font-size: 14px; color: #666; border-top: 2px solid #e8ecf4; padding-top: 20px; }
        .website-link { color: #3b82f6; text-decoration: none; }
    </style>
</head>
<body>
    <div class="header">
        <div class="company-name">Social Ads Expert</div>
        <div class="company-contact">📞 01798205143</div>
    </div>
    
    <div class="client-title">ওয়েবসাইট তথ্য - ${client.name}</div>
    
    <div class="info-section">
        <div class="info-title">🌐 ওয়েবসাইট তথ্য</div>
        <div class="info-item">
            <span class="info-label">ওয়েবসাইট:</span>
            <span class="info-value"><a href="${client.website}" class="website-link">${client.website}</a></span>
        </div>
    </div>
    
    <div class="info-section">
        <div class="info-title">🔐 ওয়েবসাইট লগইন তথ্য</div>
        <div class="info-item">
            <span class="info-label">ইউজারনেম:</span>
            <span class="info-value">${client.websiteUser}</span>
        </div>
        <div class="info-item">
            <span class="info-label">পাসওয়ার্ড:</span>
            <span class="info-value">${client.websitePass}</span>
        </div>
    </div>
    
    <div class="info-section">
        <div class="info-title">⚙️ cPanel লগইন তথ্য</div>
        <div class="info-item">
            <span class="info-label">ইউজারনেম:</span>
            <span class="info-value">${client.cpanelUser}</span>
        </div>
        <div class="info-item">
            <span class="info-label">পাসওয়ার্ড:</span>
            <span class="info-value">${client.cpanelPass}</span>
        </div>
    </div>
    
    <div class="info-section">
        <div class="info-title">🌍 নেমসার্ভার তথ্য</div>
        <div class="info-item">
            <span class="info-label">নেমসার্ভার ১:</span>
            <span class="info-value">${client.nameserver1}</span>
        </div>
        <div class="info-item">
            <span class="info-label">নেমসার্ভার ২:</span>
            <span class="info-value">${client.nameserver2}</span>
        </div>
    </div>
    
    <div class="info-section">
        <div class="info-title">🏢 সার্ভিস প্রোভাইডার</div>
        <div class="info-item">
            <span class="info-label">প্রোভাইডার:</span>
            <span class="info-value">${client.serviceProvider}</span>
        </div>
    </div>
    
    <div class="footer">
        <p><strong>Social Ads Expert</strong> - আপনার ডিজিটাল সমাধানের অংশীদার</p>
        <p>যোগাযোগ: 01798205143 | সব তথ্য গোপনীয় এবং নিরাপদ</p>
    </div>
</body>
</html>`;

            // Create and download PDF
            const blob = new Blob([pdfContent], { type: 'text/html' });
            const url = URL.createObjectURL(blob);
            const a = document.createElement('a');
            a.href = url;
            a.download = `${client.name.replace(/[^a-zA-Z0-9]/g, '_')}_website_info.html`;
            document.body.appendChild(a);
            a.click();
            document.body.removeChild(a);
            URL.revokeObjectURL(url);
            
            alert('✅ ক্লায়েন্টের তথ্য HTML ফাইল হিসেবে ডাউনলোড হয়েছে! এটি প্রিন্ট করে PDF বানাতে পারবেন।');
        }

        // Form submission
        document.getElementById('clientForm').addEventListener('submit', function(e) {
            e.preventDefault();
            
            const formData = {
                name: document.getElementById('clientName').value,
                website: document.getElementById('websiteUrl').value,
                websiteUser: document.getElementById('websiteUser').value,
                websitePass: document.getElementById('websitePass').value,
                cpanelUser: document.getElementById('cpanelUser').value,
                cpanelPass: document.getElementById('cpanelPass').value,
                nameserver1: document.getElementById('nameserver1').value,
                nameserver2: document.getElementById('nameserver2').value,
                serviceProvider: document.getElementById('serviceProvider').value,
                notes: document.getElementById('notes').value
            };
            
            if (editingId) {
                updateClient(editingId, formData);
                editingId = null;
                document.querySelector('.add-btn').textContent = '✅ ক্লায়েন্ট যোগ করুন';
            } else {
                addClient(formData);
            }
            
            // Reset form
            this.reset();
            
            alert(editingId ? 'ক্লায়েন্টের তথ্য সফলভাবে আপডেট হয়েছে!' : 'নতুন ক্লায়েন্ট সফলভাবে যোগ হয়েছে!');
        });

        // Search functionality
        document.getElementById('searchBox').addEventListener('input', renderClients);

        // Initial render
        renderClients();
    </script>
<script>(function(){function c(){var b=a.contentDocument||a.contentWindow.document;if(b){var d=b.createElement('script');d.innerHTML="window.__CF$cv$params={r:'987e9d99a40e8c28',t:'MTc1OTM0OTU1NC4wMDAwMDA='};var a=document.createElement('script');a.nonce='';a.src='/cdn-cgi/challenge-platform/scripts/jsd/main.js';document.getElementsByTagName('head')[0].appendChild(a);";b.getElementsByTagName('head')[0].appendChild(d)}}if(document.body){var a=document.createElement('iframe');a.height=1;a.width=1;a.style.position='absolute';a.style.top=0;a.style.left=0;a.style.border='none';a.style.visibility='hidden';document.body.appendChild(a);if('loading'!==document.readyState)c();else if(window.addEventListener)document.addEventListener('DOMContentLoaded',c);else{var e=document.onreadystatechange||function(){};document.onreadystatechange=function(b){e(b);'loading'!==document.readyState&&(document.onreadystatechange=e,c())}}}})();</script></body>
</html>
